// Default Parameter Values

// Identity Function:
// (a function which returns the same value you pass in),
// but returns zero when invoked with undefined, or no argument

// eslint-disable-next-line import/prefer-default-export
export const orZero = (n = 0) => n;
